# ajc lint levels for this build.
#
# adviceDidNotMatch: this project only declares the aspects, it never annotates its own
# classes with @Logged, so ajc has nothing here to weave them into. The warning is correct
# and permanent, and it would hide real weaving warnings in CI output.
adviceDidNotMatch = ignore
